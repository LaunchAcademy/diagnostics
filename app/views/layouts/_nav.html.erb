<nav class="top-bar">
  <ul class="title-area">
    <li class="name">
      <h1><a href="/">MultipleChoice</a></h1>
    </li>
  </ul>

  <section class="top-bar-section">
    <ul class="right">
      <% if current_user %>
        <li><%= link_to "Welcome, #{current_user.email}!", "#" %></li>
        <li><%= link_to "View All Quizzes", quizzes_path %></li>
        <li><%= link_to "Sign out", sign_out_path %></li>
      <% else %>
        <li><%= link_to "Sign in with LaunchPass", "/auth/launch_pass" %></li>
      <% end %>
    </ul>
  </section>
</nav>
